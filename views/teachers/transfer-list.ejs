<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Eligible Teachers for Transfer</title>
</head>
<body class="bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen text-gray-900">
    <div class="container mx-auto px-4 py-10 max-w-5xl">
        <h2 class="text-4xl font-extrabold text-center text-blue-800 mb-10">
            🎓 Teachers Eligible for Transfer
        </h2>

        <% if (teachers.length > 0) { %>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-2 gap-6">
                <% teachers.forEach(t => { %>
                    <div class="bg-white rounded-2xl shadow-md border border-gray-300 p-6 transition hover:shadow-xl hover:-translate-y-1 duration-300">
                        <div class="mb-4">
                            <h3 class="text-2xl font-semibold text-blue-700 mb-1">
                                👤 <%= t.fullName %>
                            </h3>
                            <p class="text-gray-600"><strong>📧 Email:</strong> <%= t.email %></p>
                            <% if (t.avgRating) { %>
                                <p class="text-green-700 mt-2 font-medium">
                                    ⭐ <strong>Average Rating:</strong> <%= t.avgRating.toFixed(2) %> / 5
                                </p>
                            <% } %>
                        </div>
                        <form method="POST" action="/teachers/transfer/<%= t._id %>">
                            <button class="w-full bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white py-3 px-5 rounded-xl font-bold shadow-md transition">
                                🚀 Transfer Now
                            </button>
                        </form>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="bg-white p-6 rounded-xl shadow text-center text-gray-600 text-lg">
                😔 No eligible teachers found at the moment.
            </div>
        <% } %>
    </div>
</body>
</html>
