<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Top Rated Teachers - JavaCoder</title>
</head>
<body class="bg-gray-100 text-gray-900">

    <div class="container mx-auto px-4 py-8">
        <h1 class="text-4xl font-bold text-center text-blue-700 mb-10">🏆 Top Rated Teachers</h1>

        <% if (topTeachers.length > 0) { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% topTeachers.forEach(t => { %>
                    <div class="bg-white shadow-md rounded-xl p-6 hover:shadow-xl transition-all duration-300">
                        <h3 class="text-xl font-semibold text-gray-800 mb-2"><%= t.teacher.fullName %></h3>
                        <p class="text-gray-600 mb-1"><strong>Email:</strong> <%= t.teacher.email %></p>
                        <p class="text-green-600 font-medium mb-3">⭐ Average Rating: <%= t.averageRating.toFixed(2) %></p>
                        <p class="text-sm text-gray-500">Transferred on: <%= new Date(t.transferredAt).toLocaleDateString() %></p>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p class="text-center text-gray-600 text-lg">No top teachers available yet.</p>
        <% } %>
    </div>

</body>
</html>
