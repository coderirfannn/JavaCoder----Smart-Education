<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - JavaCoder</title>
    <%- include('../partials/head') %>
</head>
<body class="bg-gray-100 text-gray-800 min-h-screen flex flex-col">
    <%- include('../partials/navbar') %>

    <main class="flex-grow max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
        <h1 class="text-3xl font-bold mb-8 text-center">
            Welcome to your Dashboard, <span class="text-indigo-600"><%= user.fullName %></span>!
        </h1>

        <% if (user.role === 'student') { %>
            <div class="bg-blue-100 border border-blue-300 p-6 rounded-2xl shadow-md mb-6">
                <h2 class="text-2xl font-semibold text-blue-700 mb-2">Student Dashboard</h2>
                <p class="text-gray-700">Here you can view your courses, grades, and upcoming exams.</p>
                <!-- Add student-specific content here -->
            </div>
        <% } else if (user.role === 'teacher') { %>
            <div class="bg-green-100 border border-green-300 p-6 rounded-2xl shadow-md mb-6">
                <h2 class="text-2xl font-semibold text-green-700 mb-2">Teacher Dashboard</h2>
                <p class="text-gray-700">Here you can manage your courses, assign grades, and upload materials.</p>
                <!-- Add teacher-specific content here -->
            </div>
        <% } else if (user.role === 'admin') { %>
            <div class="bg-red-100 border border-red-300 p-6 rounded-2xl shadow-md mb-6">
                <h2 class="text-2xl font-semibold text-red-700 mb-2">Admin Dashboard</h2>
                <p class="text-gray-700">As an admin, you can manage users, roles, and other system settings.</p>
                 

            </div>
        <% } %>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
